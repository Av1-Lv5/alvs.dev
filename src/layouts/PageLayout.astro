---
// This is the layout for the root pages ex: '/[pageName]'.
import Layout from "@layouts/Layout.astro";

// types
type Props = {
	tabTitle: string;
};

// props
const { tabTitle } = Astro.props;
---

<Layout categoryTabTitle="alvs" pageTabTitle={tabTitle} showNav={true}>
	<slot />
</Layout>
